import 'package:flutter/material.dart';

class ListServicios extends StatelessWidget {
  const ListServicios({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Lista de Servicios'));
  }
}
